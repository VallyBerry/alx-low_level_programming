#include "main.h"

/**
 * *_strcat - concatenates two strings.
 * @dest: 1st character.
 * @*b: 2nd character.
 * Return: pointer.
 */
char *_strcat(char *dest, char *src)
{
	char a[], *b

	a[] = (_putchar(dest)(src));
	b = &a;
	_putchar(a[]);
	return (b);
}
