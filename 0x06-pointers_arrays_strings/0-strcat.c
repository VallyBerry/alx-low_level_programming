#include "main.h"

/**
 * *_strcat - concatenates two strings.
 * @dest: 1st character.
 * @*b: 2nd character.
 * Return: pointer.
 */
char *_strcat(char *dest, char *src)
{
	char dest, *b

	dest = _putchar(*dest, *src);
	b = &dest;
	_putchar(dest);
	return (*b);
}
