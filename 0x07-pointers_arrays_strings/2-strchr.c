#include "main.h"

/**
 * *_strchr - locates a character in a string.
 * 
 * Return: a pointer to the first occurence of gthe character c.
 */
char *_strchr(char *s, char c)
{
	if (s[] == 'c')
	{
		return (s);
	}
	else
	{
		return (NULL);
	}
}
