#include "main.h"

/**
 * more_numbers - Prints 10 times the numbers from 0 to 14.
 *
 * Return: 0
 */
void more_numbers(void)
{
	int a;

	while (a = 0, a <= 14, a++)
			{
				a++;
				_putchar("%d", a);
			}
			_putchar("\n");

			return (0);
}
