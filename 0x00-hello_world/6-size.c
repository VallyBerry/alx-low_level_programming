#include <stdio.h>

int main(void)
{
	int integerType;
	float floatType;
	double doubleType;
	char charType;

	/* 
	 * size of operator is used to evaluate
	 * the size of a variable
	 */
	printf("size of int: %ld bytes\n",sizeof(integerType));

