#include "main.h"

/**
 * _strlen_recursion - returns the length of a string.
 * @s: given variable.
 * Return: 0.
 */
int _strlen_recursion(char *s)
{
	int i;

	i = 0;
	while (s[] +i != '\0')
	{
		i++;
		_strlen_recursion(*s);
	}
	else
	{
		return (i);
	}

}
